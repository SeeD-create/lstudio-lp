(()=>{'use strict';const $=s=>document.querySelector(s);const menu=$('#menu'),nav=$('#navigation');menu.addEventListener('click',()=>{const open=menu.getAttribute('aria-expanded')!=='true';menu.setAttribute('aria-expanded',String(open));nav.classList.toggle('open',open);menu.textContent=open?'閉じる':'メニュー'});const current=location.pathname.split('/').pop()||'index.html';nav.querySelectorAll('a').forEach(a=>{if(a.getAttribute('href')===current)a.setAttribute('aria-current','page')});let stopped=matchMedia('(prefers-reduced-motion: reduce)').matches;const motion=$('#motion');function setMotion(){document.documentElement.classList.toggle('motion-off',stopped);motion.setAttribute('aria-pressed',String(stopped));motion.textContent=stopped?'動きを再開':'動きを停止'}setMotion();motion.addEventListener('click',()=>{stopped=!stopped;setMotion()});if('IntersectionObserver'in window){document.documentElement.classList.add('js-motion');const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('seen');observer.unobserve(e.target)}}),{threshold:.08});document.querySelectorAll('.reveal').forEach(e=>observer.observe(e))}if($('#finder'))$('#findbutton').addEventListener('click',()=>{const choice=$('#finder').value;if(['life','start','business','contact'].includes(choice))location.href=choice+'.html';else $('#finderstatus').textContent='今のご状況を選んでください。'});if($('#search')){const update=()=>{const q=$('#search').value.trim().toLowerCase(),cat=$('#category').value;let count=0;document.querySelectorAll('.journal-list article').forEach(a=>{a.hidden=!((cat==='all'||a.dataset.category===cat)&&(!q||a.textContent.toLowerCase().includes(q)||a.dataset.keywords.includes(q)));if(!a.hidden)count++});$('#count').textContent=count+'件の読みもの';$('#empty').hidden=count!==0};$('#search').addEventListener('input',update);$('#category').addEventListener('change',update);update()}if(current==='article.html'&&new URLSearchParams(location.search).get('view')==='guide'){document.title='このサイトの歩き方｜こより相談室・架空デモ';$('.subhead h1').innerHTML='このサイトの<br>歩き方。';$('.subhead>p:last-child').textContent='知りたいことから、迷わず次のページへ。';$('.article-photo').src='town.webp';$('.article-photo').alt='架空の街の小道・AI生成';const sections=[['相談内容から探す','暮らしの手続き、事業をはじめる、事業を続ける。ご相談内容ページには三つの入口があります。近い状況のページから、相談の準備へ進めます。'],['費用と流れを確かめる','依頼する前に知りたい費用の考え方と、相談からの流れを別々のページで確認できます。デモでは料金を設定せず、正式情報を分かりやすく伝える構成を示しています。'],['分からないときの入口','分野を決められない場合は、相談の準備画面で「まだ分からない」を選べます。準備状況と知りたいことを選んだ後に、確認と選び直しができます。実際の問い合わせは送信されません。']];document.querySelectorAll('.article>section').forEach((s,i)=>{s.querySelector('h2').textContent=sections[i][0];s.querySelector('p').textContent=sections[i][1]});document.querySelectorAll('.toc a').forEach((a,i)=>a.textContent=(i+1)+'. '+sections[i][0])}if($('#consult')){const topic=new URLSearchParams(location.search).get('topic');if(['life','start','business'].includes(topic))$('#topic').value=topic;$('#consult').addEventListener('submit',e=>{e.preventDefault();$('#selection').replaceChildren();[['相談分野','topic'],['準備状況','preparation'],['先に知りたいこと','question']].forEach(([label,id])=>{const row=document.createElement('div'),dt=document.createElement('dt'),dd=document.createElement('dd');dt.textContent=label;dd.textContent=$('#'+id).selectedOptions[0].textContent;row.append(dt,dd);$('#selection').append(row)});$('#consult').hidden=true;$('#confirmed').hidden=false;$('#step1').removeAttribute('aria-current');$('#step2').setAttribute('aria-current','step');$('#confirmed h2').tabIndex=-1;$('#confirmed h2').focus()});$('#back').addEventListener('click',()=>{$('#consult').hidden=false;$('#confirmed').hidden=true;$('#step2').removeAttribute('aria-current');$('#step1').setAttribute('aria-current','step');$('#topic').focus()})}})();
