const $=s=>document.querySelector(s);const reduced=matchMedia('(prefers-reduced-motion: reduce)');if(!reduced.matches)document.body.classList.add('js-motion');const obs=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.06});document.querySelectorAll('.reveal').forEach(e=>obs.observe(e));const menu=$('#menu'),nav=$('#nav');menu.addEventListener('click',()=>{const open=menu.getAttribute('aria-expanded')!=='true';menu.setAttribute('aria-expanded',open);nav.classList.toggle('open',open);menu.textContent=open?'CLOSE −':'MENU ＋'});nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{menu.setAttribute('aria-expanded','false');nav.classList.remove('open');menu.textContent='MENU ＋'}));document.addEventListener('keydown',e=>{if(e.key==='Escape'){menu.setAttribute('aria-expanded','false');nav.classList.remove('open');menu.textContent='MENU ＋'}});$('#motion').addEventListener('click',e=>{const stop=document.body.classList.toggle('paused');e.target.textContent=stop?'動きを再開する':'動きを止める';e.target.setAttribute('aria-pressed',stop)});document.querySelectorAll('[data-topic]').forEach(a=>a.addEventListener('click',()=>{if($('#topic'))$('#topic').value=a.dataset.topic}));const form=$('#contactform');if(form){const q=new URLSearchParams(location.search).get('topic');if(q==='social')$('#topic').value='SNS・コンテンツ';if(q==='growth')$('#topic').value='広告・メディア';form.addEventListener('submit',e=>{e.preventDefault();$('#previewtopic').textContent=$('#topic').value;$('#previewtext').textContent=$('#message').value;form.hidden=true;$('#confirm').hidden=false;$('#confirm').focus()});$('#edit').addEventListener('click',()=>{form.hidden=false;$('#confirm').hidden=true;$('#message').focus()})}document.querySelectorAll('[data-filter]').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('[data-filter]').forEach(x=>x.setAttribute('aria-pressed',x===b));document.querySelectorAll('[data-category]').forEach(x=>x.hidden=b.dataset.filter!=='all'&&x.dataset.category!==b.dataset.filter)}));