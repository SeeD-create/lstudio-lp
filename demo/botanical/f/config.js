/* F案専用。本番の接続先が確定するまで空欄のままにする。
 * VIMEO_ID: 30分の説明動画のVimeo動画ID（数字だけ）。入れるとLP内で再生される。
 *           Vimeo側の Privacy は Embed only（このドメインのみ許可）にしておく。
 * LINE_URL: 講座用の公式LINE。診断ナビのLINEと混同しない。
 * クエリ文字列やlocalStorageで「視聴済み」を偽装する処理は設けない。
 * 未設定のときは、押したときだけ「準備中」を出す。再生できたように見せない。
 * ※2026-09-20：メール登録の導線は取りやめ。先方の指示は緑ボタンの「登録不要」表記の削除だけで、
 *   メール登録を必須にする話は確定していない。確定したらここに登録先を足す。
 */
window.F_CONFIG = Object.freeze({
  LINE_URL: "",
  VIMEO_ID: ""
});
