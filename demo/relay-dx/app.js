document.documentElement.classList.add('motion-on');const reduced=matchMedia('(prefers-reduced-motion: reduce)');const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');io.unobserve(e.target)}}),{threshold:.07});document.querySelectorAll('.reveal').forEach(e=>io.observe(e));document.querySelector('#motion')?.addEventListener('click',e=>{const stop=document.documentElement.classList.toggle('halt');e.currentTarget.textContent=stop?'動きを再開':'動きを停止';e.currentTarget.setAttribute('aria-pressed',stop)});document.querySelectorAll('dialog .close').forEach(b=>b.addEventListener('click',()=>b.closest('dialog').close()));const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const contact=document.querySelector('#contact'),form=document.querySelector('#consult'),preview=document.querySelector('#cpreview');function openConsult(topic){form.hidden=false;preview.hidden=true;if([...form.theme.options].some(o=>o.value===topic))form.theme.value=topic;contact.showModal()}document.querySelectorAll('[data-contact]').forEach(b=>b.onclick=()=>openConsult(b.dataset.contact));form.onsubmit=e=>{e.preventDefault();const d=new FormData(form);form.hidden=true;preview.hidden=false;preview.innerHTML=`<h3 class="space">確認プレビュー</h3><p class="line">${esc(d.get('theme'))}</p><p class="line">${esc(d.get('phase'))}</p><p class="line">${esc(d.get('message')||'補足なし')}</p><strong>まだ送信していません。</strong><p class="tiny">本番ではこの後に既存フォームへ接続する想定です。</p><button class="btn outline space" id="editconsult">入力に戻る</button>`;document.querySelector('#editconsult').onclick=()=>{form.hidden=false;preview.hidden=true}};const cases=[['申請・承認の支援例','申請項目の統一 → 承認者と差し戻し条件の整理 → 状態一覧と通知の設計。架空シナリオです。'],['顧客・案件管理の支援例','担当者別の情報を整理 → 案件と履歴の関係を設計 → 引き継ぎの手順を共有。架空シナリオです。'],['帳票・集計の支援例','入力元を整理 → 出力項目をそろえる → 転記が必要な箇所を確認。架空シナリオです。']];document.querySelectorAll('[data-case]').forEach(b=>b.onclick=()=>{const c=cases[+b.dataset.case];form.hidden=true;preview.hidden=false;preview.innerHTML=`<h3 class="space">${c[0]}</h3><p class="space">${c[1]}</p>`;contact.showModal()});const roles=[['design','業務設計・要件整理','現場ヒアリングから、システムの要件へ。'],['build','アプリケーション開発','業務の仕組みを、動く形に。'],['lead','プロジェクト推進','関係者の期待と進行をつなぐ。']];function renderRoles(){const filter=document.querySelector('#rolefilter')?.value;const el=document.querySelector('#roles');if(!el)return;el.innerHTML=roles.filter(r=>filter==='all'||r[0]===filter).map(r=>`<article class="rolecard"><span class="en">EXPERTISE / DEMO</span><h3>${r[1]}</h3><p>${r[2]}</p><button class="textbtn" data-apply="${r[1]}">この領域で相談　↗</button></article>`).join('');el.querySelectorAll('[data-apply]').forEach(b=>b.onclick=()=>{form.message.value=b.dataset.apply+'について相談したい';openConsult('キャリア・案件登録')})}document.querySelector('#rolefilter')?.addEventListener('change',renderRoles);renderRoles();