const reduce=matchMedia('(prefers-reduced-motion: reduce)');
if(!reduce.matches){document.documentElement.classList.add('js-motion');const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');observer.unobserve(e.target)}}),{threshold:.08});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el))}
document.querySelector('.menu').addEventListener('click',e=>{const open=e.currentTarget.getAttribute('aria-expanded')!=='true';e.currentTarget.setAttribute('aria-expanded',String(open));document.querySelector('#nav').classList.toggle('open',open)});
document.querySelector('#motion').addEventListener('click',e=>{const paused=document.documentElement.classList.toggle('paused');e.currentTarget.setAttribute('aria-pressed',String(paused));e.currentTarget.textContent=paused?'動きを再開する':'動きを止める'});
const form=document.querySelector('#consult-form');
if(form){const topic=new URLSearchParams(location.search).get('topic');for(const input of form.querySelectorAll('input[name="topic"]'))if(input.value===topic)input.checked=true;form.addEventListener('submit',e=>{e.preventDefault();const data=new FormData(form);document.querySelector('#selected-topic').textContent=data.get('topic');document.querySelector('#selected-concern').textContent=data.get('concern');form.hidden=true;const confirm=document.querySelector('#confirm');confirm.hidden=false;confirm.focus()});document.querySelector('#edit').addEventListener('click',()=>{form.hidden=false;document.querySelector('#confirm').hidden=true;form.querySelector('input:checked')?.focus()})}
