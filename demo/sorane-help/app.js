document.documentElement.classList.add('js');const reduced=matchMedia('(prefers-reduced-motion: reduce)');const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('seen');io.unobserve(e.target)}}),{threshold:.08});document.querySelectorAll('.reveal').forEach(e=>io.observe(e));let stopped=reduced.matches;function setMotion(){document.documentElement.classList.toggle('motionoff',stopped);document.querySelector('#motion').textContent=stopped?'動きを再開':'動きを停止';document.querySelector('#motion').setAttribute('aria-pressed',String(stopped))}document.querySelector('#motion').onclick=()=>{stopped=!stopped;setMotion()};reduced.addEventListener('change',e=>{stopped=e.matches;setMotion()});setMotion();document.querySelectorAll('dialog .close').forEach(b=>b.onclick=()=>b.closest('dialog').close());const cf=document.querySelector('#consult-form');cf.addEventListener('submit',e=>{e.preventDefault();const fd=new FormData(cf);document.querySelector('#selection').textContent='ご相談内容：'+fd.get('service')+' ／ 時期：'+fd.get('timing');document.querySelector('#confirm').showModal()});document.querySelector('#edit').onclick=()=>document.querySelector('#confirm').close();